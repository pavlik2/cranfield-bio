

void gpu_process(float Cstart, float gStart, float C, float Cstep,
		float gamma, float gammaStep, int svm_count, int iterations,
		size_t test_size, int col1, int row1, float* array1, float* array2,
		bool classiffication);
